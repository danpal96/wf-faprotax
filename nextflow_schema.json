{
	"$schema": "http://json-schema.org/draft-07/schema",
	"type": "object",
	"demo_url": "https://github.com/danpal96/wf-faprotax/releases/download/v0.1.0/wf-faprotax-demo.tar.gz",
	"definitions": {
		"input": {
			"title": "Input Options",
			"type": "object",
			"description": "Parameters for finding and handling input data for analysis.",
			"properties": {
				"table": {
					"type": "string",
					"format": "path",
					"title": "Table",
					"description": "Abundance tables in TSV format to use.",
					"help_text": "This accepts one of two cases: (i) the path to a single TSV file; (ii) the path to a folder containing TSV files."
				},
				"groups": {
					"type": "string",
					"format": "file-path",
					"title": "FAPROTAX groups",
					"description": "Path to a file defining the groups by which to collapse the table."
				}
			},
			"required": [
				"table"
			]
		},
		"output": {
			"title": "Output Options",
			"type": "object",
			"description": "Parameters for saving and naming workflow outputs.",
			"properties": {
				"out_dir": {
					"type": "string",
					"format": "directory-path",
					"default": "output",
					"title": "Output directory",
					"description": "Directory for output of all workflow results."
				},
				"omit_unrepresented_groups": {
					"type": "boolean",
					"default": true,
					"title": "Omit unrepresented groups",
					"description": "Omits groups with zeros in all samples"
				}
			}
		},
		"misc": {
			"title": "Miscellaneous Options",
			"type": "object",
			"description": "Everything else.",
			"default": "",
			"properties": {
				"help": {
					"type": "boolean",
					"description": "Display help text.",
					"default": false,
					"hidden": true
				},
				"version": {
					"type": "boolean",
					"description": "Display version and exit.",
					"default": false,
					"hidden": true
				}
			}
		}
	},
	"allOf": [
		{
			"$ref": "#/definitions/input"
		},
		{
			"$ref": "#/definitions/output"
		},
		{
			"$ref": "#/definitions/misc"
		}
	],
	"properties": {
		"monochrome_logs": {
			"type": "boolean"
		},
		"validate_params": {
			"type": "boolean",
			"default": true
		},
		"show_hidden_params": {
			"type": "boolean"
		}
	}
}
